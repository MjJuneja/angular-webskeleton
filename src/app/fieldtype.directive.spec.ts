import { FieldtypeDirective } from './fieldtype.directive';

describe('FieldtypeDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldtypeDirective();
    expect(directive).toBeTruthy();
  });
});
